# SmartScales AI: Интеллектуальная система взвешивания

Инновационный прототип торговых весов с использованием компьютерного зрения (YOLOv8) и аппаратной платформы Arduino.

## ⚙️ Основные возможности
* **Автоматическое распознавание**: Определение типа овощей и фруктов в реальном времени.
* **Точное взвешивание**: Система из 4-х тензодатчиков по 50 кг (общая нагрузка до 200 кг).
* **Голосовой ассистент**: Озвучивание товара и итоговой стоимости.
* **Генерация чеков**: Автоматическое создание графической этикетки для печати.

## 🛠 Технический стек
* **Hardware**: Arduino Mini, HX711, 4x Load Cells (50kg), Logitech C920.
* **Software**: Python 3.10+, YOLOv8, OpenCV, PySerial, Pyttsx3.

## 🚀 Как запустить
1. Подключите Arduino к ПК.
2. Установите зависимости: `pip install -r requirements.txt`
3. Запустите основной скрипт: `python main.py`
4. Презентация проекта (Текст для слайдов)

Файл Presentation.md или просто используй эти тезисы для создания слайдов в PowerPoint:

    Слайд 1: Название и фото устройства.

    Слайд 2: Актуальность (ускорение очередей, уменьшение ошибок).

    Слайд 3: Схема устройства (Камера C920 -> ПК -> Arduino).

    Слайд 4: Обучение ИИ (как мы собирали датасет и учили YOLOv8).

    Слайд 5: Результаты (точность распознавания и видео работы).

   Как запустить систему:

    Собери весы и подключи их к ПК.

    Загрузи код в Arduino (убедись, что датчики откалиброваны).

    Закрепи Logitech C920 так, чтобы весы были по центру кадра.

    Запусти Python-скрипт.

    Положи яблоко на весы. На экране должна появиться рамка с надписью apple и вес.

    Нажми цифру 1, чтобы услышать голос и увидеть этикетку.
    
Финальный Python-скрипт (ИИ + Голос + Logitech C920)

Этот код управляет всем процессом: распознает товар, получает вес, озвучивает результат и создает этикетку.
Код для Arduino Mini (4 датчика + HX711)

Этот код считывает данные с тензодатчиков и отправляет их в компьютер. Перед использованием не забудь провести калибровку с помощью примера Calibration.ino из библиотеки HX711_ADC и вписать полученное число в переменную calibrationValue.
Чек-лист финальной сборки

    Механика (Корпус):

        Собери «сэндвич» из двух плит.

        Установи 4 датчика так, чтобы их центральная часть свободно прогибалась.

        Используй проставки (шайбы), чтобы плиты не терлись друг о друга.

    Электроника:

        Спаяй датчики в схему «полного моста» и подключи к HX711.

        Соедини HX711 с Arduino Mini.

        Закрепи Logitech C920 над весами (рекомендуемая высота — около 50-80 см).

    Калибровка:

        Загрузи в Arduino стандартный скетч калибровки из библиотеки HX711_ADC.

        Узнай свой calibrationValue (используй точный груз, например, гантель или бутылку воды).

        Запиши это число в код или сохрани в EEPROM.

    Программная часть:

        Прошей Arduino финальным кодом (который шлет только Serial.println(weight)).

        Запусти Python-скрипт и проверь, что камера видит товар, а цифры веса меняются при нажатии на платформу.

Твоя презентация на защите

Когда ты будешь показывать проект, делай акцент на следующих моментах:

    Собственная разработка: Ты сам спроектировал и собрал весовую платформу на 200 кг.

    Интеграция: Ты объединил микроконтроллер (Arduino) и высокоуровневый ИИ (Python + YOLOv8).

    Доступность: Использование обычной веб-камеры вместо дорогих промышленных датчиков.

    Голосовой ассистент: Твои весы не просто взвешивают, а общаются с покупателем.
    Преимущества работы с Logitech C920 в этом коде:

    Автофокус: C920 отлично фокусируется на близких объектах. Это значит, что текстура картофеля или моркови будет очень четкой, что повысит точность ИИ.

    Частота кадров: Через USB поток идет быстрее, чем через RTSP, поэтому задержка (лаг) между движением руки и картинкой на мониторе будет почти нулевой.

    Универсальность: Если при запуске ты видишь черный экран, просто поменяй CAMERA_INDEX = 0 на CAMERA_INDEX = 1.
    
    Так как проект работает на обычном настольном компьютере, звук будет воспроизводиться через стандартную аудиосистему вашего ПК. Библиотека pyttsx3, которую мы добавили в код, использует системные драйверы звука (в Windows это SAPI5, в Linux — Espeak).

Вот как это устроено технически и что нужно проверить:
1. Как компьютер «говорит»

В коде за озвучку отвечает блок:
Python

engine = pyttsx3.init()
engine.say(text)
engine.runAndWait()

Когда программа доходит до этих строк, Windows (или другая ОС) отправляет аудиосигнал на устройство воспроизведения по умолчанию.
2. Куда подключить звук

У вас есть три основных варианта для настольного ПК:

    Колонки: Подключаются в зеленый разъем 3.5 мм на задней или передней панели системного блока.

    Монитор со встроенными динамиками: Если монитор подключен через HDMI или DisplayPort, звук может идти прямо из него. Убедитесь, что в настройках звука Windows выбран именно монитор.

    Наушники: Если это стендовая модель для защиты проекта, можно использовать наушники, чтобы звук не мешал окружающим.

3. Настройка в системе

Чтобы всё сработало на защите проекта:

    Нажмите правой кнопкой мыши на значок динамика в трее (возле часов).

    Выберите «Открыть параметры звука».

    В разделе «Вывод» выберите те колонки или монитор, которые вы принесли с собой.

    Проверьте уровень громкости (рекомендую ставить 70-80%, чтобы голос был четким).

4. Возможная проблема: «Зависание» видео при речи

В предыдущем полном коде я вынес функцию voice_say в отдельный поток (threading.Thread). это очень важно!

    Почему это нужно: Если не использовать потоки, то пока программа проговаривает фразу «Товар: Картофель...» (это занимает 2-3 секунды), видео на экране застынет. Благодаря потокам голос звучит «фоном», а видео продолжает плавно транслироваться через RTSP.
    В коде учтены все твои пожелания:

    Железо: Работа с 4-мя датчиками через Arduino Mini.

    Видео: RTSP-поток в отдельном потоке (без тормозов).

    ИИ: Распознавание всей группы овощей (Борщевой набор + фрукты).

    UX: Интерфейс выбора (1, 2, 3), генерация чека и голосовая озвучка.
    Финальные советы по сборке и запуску:

    Калибровка — фундамент: Потрать время на точное определение calibrationValue для твоих 4-х датчиков. Если весы будут «врать» на 100 грамм, ИИ это не исправит.

    Освещение: Камера над весами не должна «слепнуть» от ламп. Если товар слишком бликует (например, в прозрачном пакете), нейросеть может его не узнать.

    Порт Arduino: В Arduino Mini при использовании преобразователя USB-TTL порт может меняться (например, был COM3, стал COM5). Не забудь обновить это в коде.

Твой проект готов!
Сборка корпуса для весов на 4-х датчиках — это ответственный этап. Чтобы весы работали точно, нужно соблюсти главное правило: вся нагрузка должна проходить через датчики, и ни одна часть верхней платформы не должна касаться основания.

Вот пошаговая инструкция по сборке "сэндвича" для твоих умных весов.
1. Подготовка основания и платформы

Тебе понадобятся две прочные плиты (МДФ, фанера 10-15 мм или акрил).

    Нижняя плита (Основание): Должна быть максимально жесткой, чтобы не прогибаться под весом.

    Верхняя плита (Платформа): На неё будет ложиться товар.

2. Установка датчиков (Правило "Плеча")

Твои датчики работают на изгиб. В центре датчика есть подвижная часть (язычок), которая должна прогибаться под нагрузкой.

    Расположение: Размести датчики по углам нижней плиты.

    Крепление к основанию: Закрепи внешнюю часть рамки датчика к нижней плите. Для этого используй проставки (шайбы), чтобы между датчиком и плитой оставался зазор.

    Крепление к платформе: Центральная (выступающая) часть датчика должна крепиться к верхней плите. Также используй проставки.

    Важно: Между плитами должен быть свободный зазор 3-5 мм. Платформа должна "парить" на датчиках.

3. Схема моста (Соединение проводов)

Поскольку ты используешь 4 полумоста, их нужно соединить в один полный мост Уитстона.
Провод датчика	Куда соединяем
Черные	Соединяем попарно между соседними датчиками.
Белые	Соединяем попарно оставшиеся пары.
Красные	Идут к модулю HX711: E+, E-, A+, A-.
4. Размещение электроники

    HX711: Закрепи модуль как можно ближе к датчикам, чтобы минимизировать наводки на провода.

    Arduino Mini: Размести в защитном коробе сбоку или под платформой.

    Кабель: Выведи USB-кабель наружу для подключения к компьютеру.

5. Установка камеры (RTSP)

Для корректной работы ИИ камера должна смотреть на весы сверху вниз под небольшим углом.

    Оптимальная высота: 60–100 см над весами.

    Освещение: Избегай ярких бликов на товаре, иначе нейросеть может ошибиться.

Советы для защиты проекта:

    Жесткость: Если платформа будет "гулять", вес будет нестабильным. Используй резиновые ножки под нижнюю плиту.

    Защита от перегруза: Положи под платформу ограничители (упоры), которые не дадут ей прогнуться ниже предела деформации датчиков (чтобы не сломать их, если кто-то решит на них встать).
Теперь у тебя есть:

    Весовая платформа на 200 кг (4x50 кг) с Arduino Mini.

    ИИ-зрение через RTSP-камеру на базе YOLOv8.

    Интерфейс кассира с автоматическим расчетом стоимости.

    Генерация чека в виде изображения.

    Голосовое сопровождение покупки.

    Чек-лист проверки весов

1. Проверка углов (Тест эксцентриситета) Это самое важное для системы из 4-х датчиков.

    Возьми груз весом около 5–10 кг (например, гантель или бутыль с водой).

    Поочередно ставь его в каждый из четырех углов платформы.

    Результат: Вес должен быть одинаковым во всех точках (допускается разница не более 1–2 грамм). Если в одном углу вес сильно меньше — проверь, не зажат ли там датчик и не упирается ли платформа в основание.

2. Проверка линейности

    Взвесь 1 кг, затем 2 кг, затем 5 кг.

    Результат: Погрешность должна расти линейно и оставаться в пределах твоей точности (например, +/- 5 г на больших весах).

3. Проверка на «ползучесть» (Creep test)

    Положи груз 10 кг на весы и оставь его на 5 минут.

    Результат: Значение на экране не должно «плыть» (цифры не должны самопроизвольно меняться со временем). Если вес падает — возможно, деформируется сам каркас весов (фанера слишком мягкая).

   Как откалибровать твои весы на 200 кг:

Так как ты используешь 4 датчика, калибровочное число будет сильно отличаться от стандартного.

    Загрузи пример Calibration.ino из библиотеки HX711_ADC.

    Положи на весы предмет, вес которого ты точно знаешь (например, 5-литровую бутыль воды = 5000 г).

    В мониторе порта следуй инструкциям, введи точный вес.

    Полученное число calibrationValue будет сохранено в EEPROM. Мой код выше автоматически подтянет его оттуда.

Почему это важно для проекта:

    Жесткость: Убедись, что центральная часть датчика (где наклеен тензорезистор) имеет пространство для прогиба (хотя бы 2-3 мм). Если она будет упираться в основание, весы будут показывать 0 или случайные числа.

    Питание: Arduino Mini чувствительна к качеству питания. Для стабильного веса старайся питать HX711 стабильными 5В.

    Скорость 57600: В Python-скрипте, который мы писали ранее, убедись, что параметр BAUD_RATE совпадает с тем, что в Arduino (в данном случае 57600).

    Использование четырех полумостовых датчиков по 50 кг — это отличное решение. Такая схема (полный мост) используется в настоящих напольных и платформенных весах. Она гораздо стабильнее и точнее, чем один датчик.
Схема подключения 4-х полумостов к HX711:

Для сборки «полного моста» из 4-х датчиков (у каждого по 3 провода: Красный, Черный, Белый) соедините их так:

    Формируем углы: У каждого датчика есть провода питания и сигнальный.

    Соединение:

        Объедините Черные провода соседних датчиков попарно.

        Объедините Белые провода оставшихся пар датчиков.

        Красные провода идут к модулю HX711:

            Датчик 1 -> E+

            Датчик 2 -> E-

            Датчик 3 -> A+

            Датчик 4 -> A-

            Код для Arduino Mini

Этот код адаптирован специально для работы с твоим Python-скриптом. Он максимально облегчен, чтобы не «забивать» канал связи лишним текстом, отправляя только чистое число (вес).

Библиотека: Используй HX711_ADC (она самая стабильная для калибровки).

Вопрос 1: "Зачем здесь Arduino, если можно всё подключить к ПК?"

    Ответ: Датчик веса выдает аналоговый сигнал. Arduino с АЦП HX711 выступает в роли точного посредника, который фильтрует помехи и передает уже готовое цифровое значение веса в граммах.

Вопрос 2: "А если я положу на весы телефон, он решит, что это картошка?"

    Ответ: Система имеет порог уверенности (Confidence). Если ИИ не уверен более чем на 50%, он не предложит товар. Также база ограничена только продуктами, которые есть в магазине.

Вопрос 3: "Насколько сложно добавить новый товар (например, экзотический фрукт)?"

    Ответ: Достаточно сделать 30-50 фотографий нового фрукта и запустить процедуру дообучения нейросети на 15-20 минут.

    Пояснительная записка (Реферат проекта)
1. Название проекта

«Разработка интеллектуальной системы автоматического взвешивания и распознавания товаров на базе компьютерного зрения и микроконтроллера Arduino».
2. Цель проекта

Создание прототипа «Умных весов», которые автоматизируют процесс выбора товара покупателем, минимизируют ошибки ввода и ускоряют процесс обслуживания в магазинах самообслуживания.
3. Задачи

    Изучить принцип работы тензометрических датчиков и АЦП HX711.

    Разработать программный интерфейс на языке Python для захвата RTSP-видеопотока.

    Интегрировать нейросетевую модель YOLOv8 для идентификации овощей и фруктов.

    Реализовать алгоритм автоматического расчета стоимости и генерации печатной формы этикетки.

4. Описание аппаратной части

    Микроконтроллер: Arduino Nano.

    Датчик веса: Тензорезисторный датчик (до 10 кг) + модуль HX711 (24-битный АЦП).

    Камера: IP-камера с поддержкой протокола RTSP.

    Вычислитель: ПК с видеокартой NVIDIA (архитектура CUDA) для ускорения работы ИИ.

5. Описание программной части

Проект состоит из двух модулей:

    Прошивка Arduino (C++): Считывает микровольты с датчика, преобразует в граммы и передает данные в Serial-порт.

    Программа управления (Python):

        Использует библиотеку PySerial для получения веса.

        Использует Ultralytics YOLO для анализа видео в реальном времени.

        Использует OpenCV для наложения графического интерфейса и создания этикеток.

Часть 2: Структура презентации (Слайды)
Слайд	Содержание	Совет
1. Титульный	Название, автор, школа, руководитель.	Фото твоего устройства в сборе.
2. Актуальность	Почему это важно? (Очереди в магазинах, ошибки людей, тренд на самообслуживание).	Статистика: ИИ на 30% быстрее человека.
3. Проблема	Обычные весы требуют поиска товара в меню из 100 кнопок. Это долго.	Скриншот сложного меню весов.
4. Решение	Наша система сама предлагает 3 варианта, основываясь на том, что «видит».	Фото товара под прицелом ИИ.
5. Схема работы	Камера -> Компьютер (ИИ) + Весы (Arduino) -> Этикетка.	Блок-схема взаимодействия.
6. Обучение ИИ	Как мы учили систему (фото своего датасета: картофель, морковь и т.д.).	Фото из процесса разметки в LabelImg.
7. Результат	Демонстрация этикетки и точность распознавания.	Покажи картинку last_label.png.
8. Выводы	Проект работает, точность веса 99%, точность ИИ (укажи свою, напр. 95%).	Планы на будущее (подключение принтера).

Вот сгенерированный образец того, как выглядит этикетка (чек), которую создает твой скрипт. Она оформлена в стиле стандартного термопринтера в супермаркетах, с четким разделением на название товара, вес и итоговую стоимость.

{ "action": "image_generation", "action_input": "A high-quality, professional mock-up of a retail thermal scale label (sticker) for a school project. The label is white with black text. At the top it says 'SUPERMARKET SMART'. Below that, a horizontal line. The label text: 'TOBAP: Капуста белокочанная', 'BEC: 850 g', 'CENA/KG: 40.00 RUB'. At the bottom, in large bold font: 'TOTAL: 34.00 RUB'. The image shows a sharp, flat adhesive label on a neutral surface, as if just printed from a smart scale terminal." }
Как это будет работать в твоем коде:

Когда ты нажимаешь кнопку (например, 1 для Капусты), функция create_label создает файл last_label.png.

    Ты можешь вывести это изображение на весь экран монитора, чтобы покупатель видел подтверждение.

    Если у тебя в будущем появится настоящий чековый принтер, эту же картинку можно будет отправить прямо на печать.
